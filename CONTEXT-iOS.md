# Workdesk iOS

iPhone 端的呈现约定。域语言与跨端规矩以根 [CONTEXT.md](./CONTEXT.md) 为准，本文件只记那些概念在 iPhone 上怎么呈现 —— 概念一份，呈现两种。规矩一条不改，改的只是皮。

## Language

**tab 条**:
顶部一条可横滚的导航条（沙漏 + 各分类），主线唯一的顶层导航，与 Mac 的 tab 栏同构。打开落在沙漏，点 tab 切换 —— 整屏的左右滑刻意不做：行上的左滑是删除、右缘的一划是面板，三种横向手势挤在同一屏谁都接不稳，一个手势只干一件事。
_Avoid_: 底部标签栏、分段控件

**面板**:
每一屏主列之外的那块副列，从右缘拉出、盖在主列上、看完收回。沙漏屏的面板是未排期，分类屏的面板是已完成 —— 两屏同一副样式。
_Avoid_: 抽屉、Sheet、弹窗

**把手**:
屏右缘中部常驻的一枚竖条，面板的入口：拉它或从右缘向左一划，面板滑出；面板开着时它留在面板左缘原位，推回去还是它。
_Avoid_: 悬浮按钮、边缘箭头

**输入栏**:
屏底常驻的记事入口，两屏同一副；沙漏屏的带分类选择器。
_Avoid_: 悬浮加号、新建按钮

## Notes

- **iOS 是完整对等端，含左列拖排序。** Mac 上能对待办做的事，手机上都能做 ——「这件事只能回 Mac 做」正是 [ADR-0003](./docs/adr/0003-same-powers-on-every-todo-row.md) 要拆的那类规矩的放大版。见 [ADR-0006](./docs/adr/0006-ios-full-peer-shared-core.md)。
- **范围只有主线与同步记号。** 收藏流与 AI 用量不上 iOS 界面。同步的范围不因此而变 —— 收藏照样在云端与 Mac 之间走，只是这台手机不显示它；AI 用量本就是那台 Mac 自己的事。
- **每屏一条主列，加一块拉出面板。** 沙漏屏拉出未排期，分类屏拉出已完成（整列淡化照旧）。副列不升格为视图 —— 它们回答的是主列旁边的问题（还没安排什么 / 做完了什么），不是另一个地方；摆上 tab 条就成了「看起来像分类的东西」。
- **面板的入口是右缘把手，两屏同一副。** Mac 上这两块副列都在右边，手机上就从右缘拉出 —— 空间事实原样带过来，入口不发明新位置。tab 条与输入栏各自只说自己的事，不为它挂图标：tab 条上的图标看着像 tab，输入栏上的图标把「记一件事」和「看副列」两件不相干的事挤在一条栏里，键盘一升还会跟着消失。面板盖住约七成半屏宽，左边留一条露着主列，点它或往右一划就回去。
- **记事的规矩一字不差。** 输入栏在屏底是因为键盘在屏底 —— 写字的地方该挨着手。沙漏屏记的排今天、归选择器记住的分类；分类屏记的进本分类、不排期。
- **悬停语言的触屏总替换，三处行仍同一套：** 左滑＝删除（长按菜单里也有一份）；排期入口常驻图标；单击正文＝就地改写；长按菜单 ≙ Mac 的右键菜单；同步记号点开见详情，「多久之前」写在详情里。
- **撤销窗口与 Mac 同一副占位，少一条 ⌘Z。** 占位跟着死者的原位走：行原位、tab 条原位胶囊，点「撤销」就回来（见 [ADR-0007](./docs/adr/0007-soft-delete-with-undo-window.md)）。⌘Z 是 Mac 键盘的路，手机上不发明对应物（摇一摇撤销不做 —— 那是系统文本编辑的手势，接过来管删除会两头误触发）；占位本身就是撤销的全部入口。
- **拖拽三个落点原样保留。** 落在轴上某天＝改期，落在 tab 条某分类＝换归属，落在左列另一行上＝换位置 ——「落在哪儿就是哪件事」原样成立；每件事都另有一条不靠拖拽的路（日历面板、长按菜单）。
- **改写中回车「再开一行」与 Mac 同款、同一条边界。** 写了字回车当场生一条真的同级空行、光标就位（先有行、后有字）；空着回车就是写完，只收改写；没写字就走的空行悄悄收走。接不接这一记看**行的种类**，不看在哪一屏：顶层行只有分类屏主列接（那一列的顺序由人手排，新行落在哪儿一眼看得见）；子树的行四屏都接 —— 树是同一份，步骤的先后本来就是人写出来的。轴上、未排期与已完成面板的顶层行不接 —— 与 Mac 同一条理由：同级顺序看不见、或不由人排的地方，新行落在哪儿说不清。
- **子待办这一版欠三件**（[ADR-0008](./docs/adr/0008-sub-todos-as-steps.md)，欠账也写在 `SubTodoTree.swift` 头上）：**入怀与升降级** —— iOS 的拖拽是「悬到哪当场换到哪」的活重排，没有「落在身上」这一态；照 Mac 那套按纵向位置把行分三段（上下缘是缝、中间是身）试过一轮，手感不对，退了回来 —— 等磨玻璃自绘那一轮连长按菜单一起重做。**添步骤** —— Mac 走右键「添加子待办」，iOS 没有对应物，同样等长按菜单回来；树里有了第一步之后，后续步骤走回车。树常开：看、勾、删、改写、回车再开一行、兄弟间重排在手机上全数成立。
- **只做竖屏。** 时间是纵轴，横屏没有它的题目。
- **通知永不做。** 过期是安静的事实，推送就是催办，上了它整个性格就变了（[ADR-0001](./docs/adr/0001-continuous-timeline-no-overdue.md)、[ADR-0004](./docs/adr/0004-overdue-as-a-quiet-mark.md) 一脉相承）。小组件他日可议，今天不为它留门。
- **只按 iPhone 设计。** iPad 上跑放大的 iPhone 布局即可，不为它花心思。
