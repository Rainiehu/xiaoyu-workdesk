# Workdesk

一个 macOS 个人工作台。核心是待办管理：待办沿**分类**（横轴）和**时间**（纵轴）两条正交的轴组织，另有一条独立的收藏流。

## Language

### 待办

**待办**:
一件要做的事。归属于恰好一个分类，可以选择性地被安排到某一天。
_Avoid_: 任务、事项、Todo 项

**分类**:
用户自建的一组待办，创建时输入名字。是横轴：每条待办属于且只属于一个分类。
_Avoid_: 标签、清单、项目、Tab（Tab 指界面上的控件，不指这个概念本身）

**计划日**:
一条待办被安排去做的那一天。只到天，不含时刻。可以没有 —— 没有计划日的待办不出现在沙漏视图里。
_Avoid_: 截止日、到期日、DDL（这三个都隐含「过期」，而这里不存在过期）

**完成日**:
一条待办被打勾的那一天。与计划日相互独立 —— 一条计划在周一的待办可以在周三才完成。

**创建日**:
一条待办被写下来的那一天。纯记录，不参与任何时间轴的组织，只用作未排期待办的排序依据。

**今天**:
界面上「今天是哪天」的那一个答案。一处说了算，全主线共用 —— 轴的锚点、今天/昨天/明天的写法、回车记事落在哪一天，说的永远是同一天。它自己跟着日子走，不需要谁来叫醒。
_Avoid_: 当前日期、系统时间（这两个都指向时刻，而这里只到天）

### 视图

**沙漏视图**:
横跨所有分类的时间轴，按计划日铺开，今天锚定在中间、上方是过去、下方是未来，可自由滚动。右边另有一列未排期的事，与轴分开滚。界面上只用沙漏图标表示，不带文字。
_Avoid_: 今日、日程、日历、时间线

**分类视图**:
点开一个分类看到的那一屏：左右两列，左边待完成、右边已完成，同屏可见、互不干扰。左列突出、右列整体淡化。
_Avoid_: 分类页、清单页

**主线**:
待办功能的整体入口，即沙漏视图与所有分类共同构成的那个界面。侧边栏两项之一（另一项是收藏流）。

**收藏流**:
与待办完全无关的一条独立流水，收藏链接或文字片段。

## Notes

- **不存在「过期」这个概念。** 计划日在过去而未完成的待办，就安静地留在它那一天，不冒泡、不变色、不催促。这是有意的取舍，见 [ADR-0001](./docs/adr/0001-continuous-timeline-no-overdue.md)。
- **时间在分类视图里是刻意弱化的。** 日期只是行尾一个低对比度的次要标签，不拿来分组、不设标题 —— 在分类里关心的是「有哪些事」，「排在哪天」是沙漏视图的事。
- **一行待办能做什么，不看它在哪一列。** 打勾、删除、就地改写、移到分类、拖拽，在分类视图、轴上、未排期列三处完全一样 —— 沙漏视图是打开主线默认落地的那一屏，也是最常盯着的一屏，在那儿看见一句话写错了却得先切到某个分类去改，这一趟没有道理；而「哪几件事只在哪一屏能做」本身就是一条要人记住的规矩，抵不上它挡下的那点麻烦，见 [ADR-0003](./docs/adr/0003-same-powers-on-every-todo-row.md)。唯一还看列的是**排序**：只有分类视图左列能拖着换位置，因为「位置」只在一个分类里可比。
- **三处的待办行是同一副结构**：`圈 → 正文 → Spacer → 日期/排期入口 → 删除 → 分类 tag`，差别只在哪几格是空的 —— 分类视图不画 tag（在哪个分类里不言自明），未排期列的 tag 在组头上，轴上的位置本身就是计划日、于是没有排期入口。删除永远浮在行尾那个常驻元素的左边，tag 因此钉得住，一排行的右边缘是齐的。顺序、内缩、悬停底色都定在 `TodoRowStyle.swift` 一处，三处照着来 —— 先前没有这么一处共同的定义，三行才各自漂移成了三副模样。字号是唯一按列宽走的东西：宽列用 body（轴、分类视图左列），窄列用 callout（未排期列、分类视图右列）。
- **打勾把一行点亮成它的分类色，轴上除外。** 未完成的圈一律是灰的，完成了才填成所属分类的颜色 —— 未完成时颜色还没什么好说的，而一行上只该有一处彩色抢眼。轴上不是：那儿的勾圈填整体主调的青色，与 tab 栏上的沙漏、「今天」那一组同一个颜色。理由与轴上的 tag 不着色是同一条 —— 一屏几十行，每行一个彩色勾圈就成了噪点，而那一屏该抢眼的是「今天」。未排期列仍是分类色：那儿的组头本就是彩色胶囊，颜色有出处，而勾完亮一拍就走。
- **分类的颜色是它的记号，所以色板里任意两种都得一眼看得出差别。** 14 种颜色绕色相环走一圈，末尾一种中性的石墨色；「看着像同一个颜色」和「就是同一个颜色」一样会让人认错 tab，所以相近的档次宁可不给（早先的青/薄荷/天蓝就是这么撞在一起的）。落盘的只有色名，色值定在视图层一处，每种两副（浅色外观深一档、深色外观浅一档），于是整套配色随时可以调而不动数据。新建分类按 `CategoryColor.palette` 那个跳着走的顺序取色 —— 连着建的几个因此拿到隔得最远的几种，而不是相邻的几档绿；选色盘则按色相环顺着铺，挑起来好找。
- **一屏上着色的地方要数得过来。** 分类色出现在 tab、打了勾的圈（轴上除外）、未排期列的组头、记事栏右边的分类选择器上；沙漏视图轴上每行的 tag 是光板的：没有胶囊、没有底、也不着色 —— 那儿一屏几十行，每行挂一个胶囊就成了噪点，而轴上该抢眼的是「今天」那一组。记事栏的加号同理取整体主调的青色，与 tab 栏上的沙漏一致：那一屏不属于任何分类，「记到哪儿」由它右边的分类选择器一处说清楚。
- **完成的待办不划删除线。** 做完的事读起来仍该清晰体面，它是可查的记录，不是被划掉的废稿；「退到背景里」靠整列淡化来说，不靠划掉每一行。
- **非空分类删不掉。** 分类里只要还有任何待办（无论完成与否），删除就被拒绝，一条待办也不动 —— 删除没有撤销、没有回收站，所以宁可在前面拦住，也不要事后无声地丢失待办。这条约束由 `Store` 保证，不依赖视图层自觉。疏通的路子是待办行上的「移到分类」或把它们删掉；空分类则直接删，不弹确认。删到一个分类不剩是允许的，那时回到引导空态。
- **改分类只改归属。** 待办换到别的分类，计划日、创建日、完成日与完成状态都不变 —— 横轴上的移动不碰纵轴。入口有两个：待办行右键的「移到分类」，以及把这一行拖到 tab 栏上另一个分类上 —— 三处的行都有这两个入口。沙漏 tab 不接落点：它不代表任何分类，没有「归到沙漏」这回事。移过去的那条落在新分类左列的最上面 —— 它在原来那列里的位置到了新分类没有意义，而刚移过去的那条该一眼看得见。
- **分类视图左列的顺序归使用者自己拖。** 拖着一行放到另一行上，它就落到那个位置，其余的依次让开。日期只是初值：旧数据照老规矩（已排期按计划日，未排期按创建日从新到旧）补一遍位置，此后排期、改期都不再动这一列的顺序，见 [ADR-0002](./docs/adr/0002-hand-made-order-in-category-view.md)。新记下的待办落在最上面。右列不由人排，仍按完成日从新到旧 —— 那是一份记录。
- **改写正文只改那一句。** 待办的字可以就地改写（单击那一行，或右键「改写」），归属分类、计划日、创建日、完成日与完成状态都不变 —— 不必删掉重记，那样会把这些一起丢掉。改成空白不算改，原文留着：删除是另一条路，不该由「把字删光」触发。Esc 放弃这次改写，点到别处去也算改完 —— 一次改写没有「没保存」这个下场。改写时行上让开的只有删除：手正放在字上，旁边不该还摆着一个删得掉整条的按钮。三处的待办行都是这一套。
- **未排期的事在沙漏视图右边单开一列。** 所有未完成且没有计划日的待办都在那儿，按分类分组（组的顺序就是 tab 栏的顺序，组内与分类视图左列同序），自己滚自己的 —— 轴回答「排在哪天」，这一列回答「还有什么没安排」。它们不在轴上，但不该因此看不见：未排期是个正常状态，不是缺失。排上计划日的一刻它就离开这一列出现在轴上；从这一列拖一条到轴上某一天，就是排期。打勾同样让它离开，只是没有看得见的去处（做完的事在分类视图右列），所以那一下走得慢一点：圈先填实亮一拍，再淡出。
- **拖一行待办，落在哪儿就是哪件事。** 落在轴上某一天是改期（纵轴），落在 tab 栏某个分类上是改归属（横轴），落在分类视图左列另一行上是换位置。一次只动一样：改期不碰分类，改归属不碰三个日子与完成状态。三处的行拖起来都是这一套，抓的是同一样东西，只看松手落在哪儿。
- **计划日、完成日、创建日是三个独立的时间**，不可互相顶替。旧版本曾用创建日兼作计划日（「挪到今天」直接改写创建时间），这是已被废弃的做法。
- **「今天」自己跟着日子走，不等人来叫醒。** 这台机器上的这个窗口常常一开就是几天，跨过零点之后界面若还停在昨天，看到的就是一份对不上的安排 —— 更糟的是那时在记事栏敲回车，这条待办会被排到昨天去。所以「今天」是一处说了算的一个值，日子一变当场跟上，不管窗口在不在前台、也不管有没有人正看着；跟上的同时轴一律重新锚回今天，就跟刚打开一样。它只在**日子**变了时才动 —— 时刻一直在走，但那不是它要说的事。
- **三者都带着完整时刻存储，界面上一律只显示到天。** 截断是显示层的事，不写进数据 —— 底下留着时刻，同一天里的几条才分得出先后（排序、以后要改粒度都靠它）；界面上则没有任何地方问「几点」。因此：格式化时截到天，判断「是不是同一天」用 `Calendar` 比而不是 `==`，按天分组用 `dayStart` 当键。计划日由日历选出来，天然落在当天零点，这是它的值，不是一次截断。
